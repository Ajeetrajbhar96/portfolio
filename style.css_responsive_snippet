/* ================= RESPONSIVE FIXES ================= */

@media (max-width: 991px) {
    header {
        padding: 20px;
    }

    section {
        padding: 60px 5%;
    }

    .home {
        padding: 0 5%;
    }

    .home img {
        width: 300px;
        height: 300px;
    }

    nav {
        width: 95%;
    }
}

@media (max-width: 768px) {

    /* --- GLOBAL --- */
    h1 {
        font-size: 42px !important;
    }

    h2 {
        font-size: 32px !important;
    }

    h3 {
        font-size: 24px !important;
    }

    section {
        padding: 50px 5%;
    }

    /* --- NAVBAR --- */
    nav {
        padding: 10px 20px;
        border-radius: 50px;
    }

    .menu-toggle {
        display: block;
        z-index: 1001;
    }

    nav ul {
        position: fixed;
        /* Changed from absolute to fixed for better mobile coverage */
        top: 0;
        left: 0;
        width: 100%;
        height: 100vh;
        background: rgba(2, 6, 23, 0.98);
        backdrop-filter: blur(20px);
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 30px;
        transform: translateY(-100%);
        transition: 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        border-radius: 0;
        z-index: 999;
    }

    nav ul.active {
        display: flex;
        transform: translateY(0);
    }

    nav ul li a {
        font-size: 22px;
        color: white;
    }

    /* --- HOME --- */
    .home {
        flex-direction: column-reverse; /* Text bottom, Image top is standard but reference might differ. Let's stick to column for stacking */
        justify-content: center;
        text-align: center;
        padding-top: 120px; /* Space for navbar */
        gap: 40px;
    }

    .home img {
        width: 280px;
        height: 280px;
    }

    .home-text {
        max-width: 100%;
    }

    .btns {
        display: flex;
        flex-direction: column;
        gap: 15px;
        padding: 0 40px;
    }

    .btns button {
        margin: 0;
        width: 100%;
    }

    .social-icons {
        justify-content: center;
    }

    /* --- SKILLS & TOOLS --- */
    .skills-grid {
        grid-template-columns: repeat(auto-fit, minmax(110px, 1fr)); /* Smaller min-width for mobile */
        gap: 15px;
    }

    .skill-card {
        padding: 15px;
    }

    .skill-card i {
        font-size: 40px;
    }
    
    .skill-category {
        font-size: 20px;
        width: 100%;
    }

    /* --- ABOUT --- */
    .about-container {
        flex-direction: column;
        text-align: center;
    }

    .about-right p {
        text-align: justify; /* Better readability */
        text-align-last: center; 
    }

    /* --- CONTACT --- */
    .contact-container {
        flex-direction: column;
    }
    
    .contact-form {
        padding: 25px;
    }
}

@media (max-width: 450px) {
    .home h1 {
        font-size: 32px !important;
    }
    
    .home img {
        width: 220px;
        height: 220px;
    }
    
    .btns {
        padding: 0;
    }
}
